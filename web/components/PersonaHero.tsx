import { Persona } from "@/types";


export default function PersonaHero({ persona }: { persona: Persona }) {
    return (
        <section className="relative overflow-hidden py-16 px-10 h-[100vh]" id="about">

        </section>
    )
};